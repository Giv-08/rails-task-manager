<h1>My task</h1>
<ul>
<%= form_with model: @task do |f| %>
<% @tasks.each do |task| %>
<li>
<a href="<%= task_path(task) %>">
  <%= f.check_box :completed %>
  <%= f.label task.title %></a>
  </li>
  <% end %>
<% end %>
</ul>
